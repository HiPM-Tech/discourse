# Discourse 生产环境 Docker Compose 配置
# 使用方式: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# 注意：此配置假设你已经在外部运行 PostgreSQL 和 Redis
# 配置外部数据库连接请在 .env 文件中设置

version: "3.8"

services:
  # 生产环境使用 Nginx 作为反向代理
  nginx:
    image: nginx:alpine
    container_name: discourse-nginx
    ports:
      - "${DISCOURSE_HTTP_PORT:-80}:80"
      - "${DISCOURSE_HTTPS_PORT:-443}:443"
    volumes:
      - ./docker/nginx/nginx.prod.conf:/etc/nginx/nginx.conf:ro
      - ./docker/nginx/ssl:/etc/nginx/ssl:ro
      - discourse-uploads:/var/www/discourse/public/uploads:ro
      - discourse-logs:/var/log/nginx
    depends_on:
      app:
        condition: service_healthy
    networks:
      - discourse-network
    restart: unless-stopped

  app:
    # 生产环境使用预构建镜像而非本地构建
    image: ${DISCOURSE_IMAGE:-ghcr.io/hipm-tech/discourse:latest}
    build: null
    container_name: discourse-app
    environment:
      RAILS_ENV: production
      UNICORN_WORKERS: ${UNICORN_WORKERS:-8}
      UNICORN_SIDEKIQS: ${UNICORN_SIDEKIQS:-2}
      DISCOURSE_ENABLE_CORS: "true"
      DISCOURSE_SERVE_STATIC_ASSETS: "true"
      # 生产环境强制使用安全密钥
      DISCOURSE_SECRET_KEY_BASE: ${DISCOURSE_SECRET_KEY_BASE:?错误: 生产环境必须设置 DISCOURSE_SECRET_KEY_BASE}
      DISCOURSE_DB_PASSWORD: ${DISCOURSE_DB_PASSWORD:?错误: 生产环境必须设置 DISCOURSE_DB_PASSWORD}
      # 外部数据库配置
      DISCOURSE_DB_HOST: ${DISCOURSE_DB_HOST:-postgres}
      DISCOURSE_DB_PORT: ${DISCOURSE_DB_PORT:-5432}
      DISCOURSE_DB_NAME: ${DISCOURSE_DB_NAME:-discourse}
      DISCOURSE_DB_USERNAME: ${DISCOURSE_DB_USERNAME:-discourse}
      DISCOURSE_REDIS_HOST: ${DISCOURSE_REDIS_HOST:-redis}
      DISCOURSE_REDIS_PORT: ${DISCOURSE_REDIS_PORT:-6379}
      DISCOURSE_REDIS_PASSWORD: ${DISCOURSE_REDIS_PASSWORD:-}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '1.0'
          memory: 1G
    networks:
      - discourse-network
    restart: unless-stopped

  sidekiq:
    image: ${DISCOURSE_IMAGE:-ghcr.io/hipm-tech/discourse:latest}
    build: null
    container_name: discourse-sidekiq
    environment:
      RAILS_ENV: production
      DISCOURSE_SECRET_KEY_BASE: ${DISCOURSE_SECRET_KEY_BASE}
      DISCOURSE_DB_PASSWORD: ${DISCOURSE_DB_PASSWORD}
      # 外部数据库配置
      DISCOURSE_DB_HOST: ${DISCOURSE_DB_HOST:-postgres}
      DISCOURSE_DB_PORT: ${DISCOURSE_DB_PORT:-5432}
      DISCOURSE_DB_NAME: ${DISCOURSE_DB_NAME:-discourse}
      DISCOURSE_DB_USERNAME: ${DISCOURSE_DB_USERNAME:-discourse}
      DISCOURSE_REDIS_HOST: ${DISCOURSE_REDIS_HOST:-redis}
      DISCOURSE_REDIS_PORT: ${DISCOURSE_REDIS_PORT:-6379}
      DISCOURSE_REDIS_PASSWORD: ${DISCOURSE_REDIS_PASSWORD:-}
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
    networks:
      - discourse-network
    restart: unless-stopped

  # 可选: 自动备份服务
  backup:
    image: ${DISCOURSE_IMAGE:-ghcr.io/hipm-tech/discourse:latest}
    container_name: discourse-backup
    command: >
      sh -c "
        while true; do
          echo 'Starting backup...';
          bundle exec rake db:backup;
          # 保留最近7天的备份
          find /var/www/discourse/public/backups -name '*.tar.gz' -mtime +7 -delete;
          echo 'Backup completed. Sleeping for 24 hours...';
          sleep 86400;
        done
      "
    environment:
      RAILS_ENV: production
      DISCOURSE_SECRET_KEY_BASE: ${DISCOURSE_SECRET_KEY_BASE}
      DISCOURSE_DB_PASSWORD: ${DISCOURSE_DB_PASSWORD}
      # 外部数据库配置
      DISCOURSE_DB_HOST: ${DISCOURSE_DB_HOST:-postgres}
      DISCOURSE_DB_PORT: ${DISCOURSE_DB_PORT:-5432}
      DISCOURSE_DB_NAME: ${DISCOURSE_DB_NAME:-discourse}
      DISCOURSE_DB_USERNAME: ${DISCOURSE_DB_USERNAME:-discourse}
    volumes:
      - discourse-backups:/var/www/discourse/public/backups
    networks:
      - discourse-network
    restart: unless-stopped
    profiles:
      - backup

networks:
  discourse-network:
    driver: bridge
